<odoo>
  <data>
    <!-- BoM list view -->
    <record id="view_bom_tree_inherit" model="ir.ui.view">
      <field name="name">mrp.bom.tree.inherit</field>
      <field name="model">mrp.bom</field>
      <field name="inherit_id" ref="mrp.mrp_bom_tree_view"/>
      <field name="arch" type="xml">
        <xpath expr="//tree/field[3]" position="after">
          <field name="barcode"/>
        </xpath>
        <xpath expr="//tree" position="inside">
          <field name="bom_cost"/>
        </xpath>
      </field>
    </record>

    <!-- BoM form view -->
    <!-- adding barcode to bom lines -->
    <record id="view_bom_form_inherit" model="ir.ui.view">
      <field name="name">mrp.bom.form.inherit</field>
      <field name="model">mrp.bom</field>
      <field name="inherit_id" ref="mrp.mrp_bom_form_view"/>
      <field name="arch" type="xml">
        <xpath expr="//field[@name='bom_line_ids']/tree/field[@name='product_id']" position="after">
          <field name="barcode"/>
        </xpath>
      </field>
    </record>


  </data>
</odoo>
